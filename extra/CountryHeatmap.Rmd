---
title: "Country heatmap"
author: "Michael Beer"
output: html_document
---

```{r load-packages, message = FALSE}
library(tidyverse)
library("ggplot2")
library("sf")
library("rnaturalearth")
library("rnaturalearthdata")
theme_set(theme_dark())
theme(
  
)
suicides <- read_csv("../data/master.csv")
```

```{r world}
world <- ne_countries(scale = "medium", returnclass = "sf") %>%
  filter(type == "Sovereign country" | type == "Country")
```

```{r total-suicides-country-per-pop}
  suicide_rates <- suicides %>%
  group_by(country) %>% 
  rename(suicides_per_100k_pop = 'suicides/100k pop',
         name_long = 'country') %>%
  summarise(suicide_rates = sum(suicides_per_100k_pop)) %>% 
  arrange(desc(suicide_rates))
```

```{r world-map-suicides}
  suicidemap <- merge(world, suicide_rates, by="name_long", all=TRUE)
```

```{r heatmap}
  ggplot(data = suicidemap) +
    geom_sf(aes(fill = suicide_rates)) +
        labs(title = "Suicide Rates per Country") +
           scale_fill_continuous(low="thistle2", high="darkred", 
                       guide="colorbar",na.value="black")
```